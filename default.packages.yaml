# Mock default.packages.yaml for local e2e testing
# This file is only used when running tests locally outside CI.
# The real file is injected during CI deployment at this same location.
packages:
  enabled:
    - package: "@backstage-community/plugin-rbac"
    - package: "@backstage/plugin-kubernetes"
    - package: "@janus-idp/backstage-plugin-3scale-backend"
    - package: "@janus-idp/backstage-plugin-argocd"
    - package: "@janus-idp/backstage-plugin-ocm"
  disabled:
    - package: "@backstage/plugin-example"
