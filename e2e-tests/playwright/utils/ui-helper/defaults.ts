export const DEFAULT_CLICK_BUTTON_OPTIONS = {
  exact: true,
  force: false,
} as const;

export const DEFAULT_CLICK_BUTTON_BY_TEXT_OPTIONS = {
  exact: true,
  timeout: 10000,
  force: false,
} as const;

export const DEFAULT_VERIFY_LINK_OPTIONS = {
  exact: true,
  notVisible: false,
} as const;
