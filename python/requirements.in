# to use this file, `pip install pip-tools -U` first (need >= 7.3), then
# pip-compile --allow-unsafe --generate-hashes --output-file=requirements.txt --strip-extras requirements.in

# TechDocs MkDocs stack — transitive pins come from mkdocs-techdocs-core.
mkdocs-techdocs-core==1.7.1

# Hermeto (allow_binary=false) prefetches one shared requirements-build.txt.
# urllib3>=2.5 and platformdirs>=4.5 need setuptools-scm>=8 via hatch-vcs,
# but python-dateutil's sdist still requires setuptools_scm<8.
urllib3>=2.2.2,<2.5
platformdirs>=3.11,<4.5
