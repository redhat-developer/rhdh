name: AI Assistant Rulesync

on:
  pull_request:
    paths:
      - '.rulesync/**'
      - '.cursor/**'
      - '.cursorignore'
      - '.claude/**'
      - '.opencode/**'
      - 'opencode.jsonc'
      - 'rulesync.jsonc'
      - '.rulesyncignore'
      - '.lintstagedrc.cjs'
      - 'package.json'
      - 'yarn.lock'
  push:
    branches:
      - 'main'
      - 'release-[0-9]+.[0-9]+'
    paths:
      - '.rulesync/**'
      - '.cursor/**'
      - '.cursorignore'
      - '.claude/**'
      - '.opencode/**'
      - 'opencode.jsonc'
      - 'rulesync.jsonc'
      - '.rulesyncignore'
      - '.lintstagedrc.cjs'
      - 'package.json'
      - 'yarn.lock'

jobs:
  rulesync:
    name: Generate and check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version-file: ".nvmrc"

      - name: Install dependencies
        run: yarn install

      - name: Check rulesync configuration and generated files
        run: yarn rulesync:check
