[pytest]
markers =
    integration: marks tests that require external dependencies (npm, skopeo, openssl, etc.)
    slow: marks tests that are slow to run (typically integration tests with real downloads)

