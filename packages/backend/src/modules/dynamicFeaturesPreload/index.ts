/**
 * Dynamic-features preload internals — collector, handler, store, paths.
 *
 * Wired by {@link dynamicFeaturesResolver} in the parent modules directory.
 * No public exports; all access is via direct imports from the plugin.
 */
